# This file must be used with "source bin/activate" *from bash*
# you cannot run it directly

# It performs the following actions:
# * exports DISTRIBUTION_BASE_DIR in the environment, so that it can be used
#   in other scripts here in bin/ folder.
# * adds local bin/ folder to the PATH
# * adds a "deactivate" command to restore the PATH to its previous state

# Highly inspired by Python-virtualenv's bin/activate

deactivate () {
    # reset old environment variables
    if [ -n "$_OLD_VIRTUAL_PATH" ] ; then
        PATH="$_OLD_VIRTUAL_PATH"
        export PATH
        unset _OLD_VIRTUAL_PATH
    fi
    
    # This should detect bash and zsh, which have a hash command that must
    # be called to get it to forget past commands.  Without forgetting
    # past commands the $PATH changes we made may not be respected
    if [ -n "$BASH" -o -n "$ZSH_VERSION" ] ; then
        hash -r
    fi
}

DISTRIBUTION_BASE_DIR=/home/benoit/web/drupal-distribution
export DISTRIBUTION_BASE_DIR

# Adapt this to your local environment.
#export DRUSH_PHP=/usr/bin/php5
#alias php='bin/php'

_OLD_VIRTUAL_PATH="$PATH"
PATH="$DISTRIBUTION_BASE_DIR/bin:$PATH"
export PATH

# This should detect bash and zsh, which have a hash command that must
# be called to get it to forget past commands.  Without forgetting
# past commands the $PATH changes we made may not be respected
if [ -n "$BASH" -o -n "$ZSH_VERSION" ] ; then
    hash -r
fi
